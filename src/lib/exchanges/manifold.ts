// Manifold adapter removed — no longer surfaced in the screener.
// File kept (instead of deleted) because the bash sandbox can't unlink
// files in the OneDrive mount. Safe to delete from File Explorer.
export {};
